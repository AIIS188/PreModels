# 外部数据对接状态报告

**检查日期**: 2026-03-08  
**检查人**: AIIS188  
**系统**: PreModels v2 滚动优化系统  

---

## 概述

本报告检查 PreModels 模型所需的外部数据对接情况，识别已对接和未对接的数据源。

---

## 模型需要的数据清单

### 1. 合同数据 (Contracts)

**用途**: 合同总量、有效期、允许品类、单价

**当前状态**: 未对接 (硬编码)

**位置**: `rolling_optimizer.py:134`

```python
def _load_contracts(self) -> List[Contract]:
    # TODO: 从 PD API 获取
    # 临时硬编码
    return [
        Contract(cid="C1", receiver="R1", Q=520.0, ...),
        Contract(cid="C2", receiver="R2", Q=900.0, ...),
    ]
```

**PD API 支持**: 已实现

- `GET /api/v1/contracts/` - 获取合同列表
- `GET /api/v1/contracts/id/{id}` - 获取合同详情

**对接工作量**: 低 (1-2 小时)

---

### 2. 产能预测 (Capacity Forecast)

**用途**: 各仓库各品类的每日最大发货量

**当前状态**: 未对接 (硬编码)

**位置**: `rolling_optimizer.py:143`

```python
def _load_cap_forecast(self, today: int, H: int) -> Dict:
    # TODO: 从产能系统获取
    # 临时硬编码
    cap_today = {
        ("W1", "A"): 220.0, ("W1", "B"): 60.0,
        ("W2", "A"): 80.0,  ("W2", "B"): 220.0,
        ...
    }
```

**PD API 支持**: 不支持

**问题**: PD 系统没有产能预测功能

**解决方案**:

1. 在 PD 系统中新增产能管理模块
2. 使用 Excel/配置文件手动维护
3. 接入第三方产能预测系统

**对接工作量**: 高 (需新开发)

---

### 3. 估重画像 (Weight Profile)

**用途**: 各线路的估重分布 (mu, hi)

**当前状态**: 未对接 (硬编码)

**位置**: `rolling_optimizer.py:161`

```python
def _load_weight_profile(self) -> Dict:
    # TODO: 从历史磅单数据学习
    return {
        ("W1", "R1", "A"): (32.0, 35.0),
        ...
    }
```

**PD API 支持**: 部分支持

- `GET /api/v1/weighbills/` - 可获取历史磅单数据
- 需要从历史数据中学习统计

**解决方案**: 实现学习算法，从 PD 历史磅单数据中计算各线路的 mu/hi

**对接工作量**: 中 (3-4 小时)

---

### 4. 延迟分布 (Delay Profile)

**用途**: 各线路的到货延迟概率分布

**当前状态**: 未对接 (硬编码)

**位置**: `rolling_optimizer.py:173`

```python
def _load_delay_profile(self) -> Dict:
    # TODO: 从历史磅单数据学习
    return {
        ("W1", "R1"): {0: 0.03, 1: 0.90, 2: 0.04, 3: 0.03},
        ...
    }
```

**PD API 支持**: 部分支持

- `GET /api/v1/weighbills/` - 可获取磅单日期
- `GET /api/v1/deliveries/` - 可获取报货日期
- 需要计算延迟分布

**解决方案**: 实现学习算法，对比报货日期和实际磅单日期，统计延迟分布

**对接工作量**: 中 (3-4 小时)

---

### 5. 磅单数据 (Weighbills) - 已对接

**用途**: 获取已确认的到货量

**当前状态**: 已对接

**位置**: `api_client.py`

```python
def get_weighbills(...) -> List[WeighbillData]
def get_weighbills_today(today: str, cid: str = None) -> List[WeighbillData]
```

**PD API 支持**: 完全支持

- `GET /api/v1/weighbills/` - 获取磅单列表
- `GET /api/v1/weighbills/delivery/{id}` - 获取指定报单的磅单

**状态**: 完成

---

### 6. 报货单数据 (Deliveries) - 已对接

**用途**: 获取在途报单列表

**当前状态**: 已对接

**位置**: `api_client.py`

```python
def get_deliveries(...) -> List[DeliveryData]
```

**PD API 支持**: 完全支持

- `GET /api/v1/deliveries/` - 获取报货单列表
- `GET /api/v1/deliveries/{id}` - 获取报货单详情

**状态**: 完成

---

### 7. 创建报货单 (Create Delivery) - 已对接

**用途**: 执行发货计划，创建报货单

**当前状态**: 已对接

**位置**: `api_client.py`

```python
def create_delivery(delivery: Dict) -> Dict
```

**PD API 支持**: 完全支持

- `POST /api/v1/deliveries/json` - 创建报货单

**状态**: 完成

---

### 8. 仓库发货能力评估 - 未对接

**用途**: 评估各仓库的发货能力，用于优化模型

**当前状态**: 未对接

**PD API 支持**: 不支持

**问题**: PD 系统没有仓库发货能力评估功能

**需要的数据**:

- 仓库历史发货量统计
- 仓库日均发货能力
- 仓库峰值发货能力
- 仓库设备/人力约束

**解决方案**:

1. 在 PD 系统中新增仓库能力评估模块
2. 从历史发货数据中统计分析
3. 手动配置仓库能力参数

**对接工作量**: 高 (需新开发，2-3 天)

---

### 9. 模型预测部分 - 未对接

**用途**: 基于历史数据预测未来趋势

**当前状态**: 未对接

**PD API 支持**: 不支持

**问题**: PD 系统没有预测功能

**需要的预测**:

- 产能预测 (见第 2 项)
- 需求预测 (合同到期前的需求高峰)
- 价格趋势预测 (可选)

**解决方案**:

1. 集成机器学习库 (如 scikit-learn, prophet)
2. 基于历史数据训练预测模型
3. 提供预测 API 接口

**对接工作量**: 高 (需新开发，3-5 天)

---

## 对接状态总结

### 已完成 (3 项)

| 数据项 | 状态 | 接口 | 说明 |
|--------|------|------|------|
| 磅单数据 | 已完成 | `GET /api/v1/weighbills/` | 获取已确认到货 |
| 报货单数据 | 已完成 | `GET /api/v1/deliveries/` | 获取在途报单 |
| 创建报货单 | 已完成 | `POST /api/v1/deliveries/json` | 执行发货计划 |

### 未对接 (6 项)

| 数据项 | 状态 | 优先级 | 工作量 | 说明 |
|--------|------|--------|--------|------|
| 合同数据 | 未对接 | 高 | 低 | PD API 已支持，需集成 |
| 产能预测 | 未对接 | 高 | 高 | PD 不支持，需新开发 |
| 估重画像 | 未对接 | 中 | 中 | 需从历史数据学习 |
| 延迟分布 | 未对接 | 中 | 中 | 需从历史数据学习 |
| 仓库能力评估 | 未对接 | 高 | 高 | PD 不支持，需新开发 |
| 模型预测 | 未对接 | 中 | 高 | PD 不支持，需新开发 |

---

## 优先级建议

### 高优先级 (立即实施)

1. **合同数据对接** (1-2 小时)
   - PD API 已支持
   - 实现简单，收益明显
   - 减少硬编码

2. **仓库发货能力评估** (2-3 天)
   - 对优化模型影响重大
   - 需要新开发功能
   - 建议作为独立模块

### 中优先级 (近期实施)

3. **估重画像学习** (3-4 小时)
   - 基于历史磅单数据
   - 提高估重准确性
   - 减少人工配置

4. **延迟分布学习** (3-4 小时)
   - 基于历史数据对比
   - 提高在途预测准确性
   - 减少人工配置

### 低优先级 (远期规划)

5. **产能预测** (需新开发)
   - 需要独立的产能管理系统
   - 或者手动维护配置文件
   - 建议作为长期目标

6. **模型预测** (需新开发)
   - 需要机器学习能力
   - 数据量要求高
   - 建议作为长期目标

---

## 实施建议

### 第一阶段 (本周)

- [ ] 对接合同数据 API
- [ ] 实现估重画像学习算法
- [ ] 实现延迟分布学习算法

### 第二阶段 (下周)

- [ ] 设计仓库能力评估模块
- [ ] 实现仓库能力统计分析
- [ ] 集成到优化模型

### 第三阶段 (本月)

- [ ] 评估产能预测需求
- [ ] 设计预测模型架构
- [ ] 收集训练数据

---

## 代码位置

### 需要修改的文件

1. `v2/rolling_optimizer.py`
   - `_load_contracts()` - 第 134 行
   - `_load_cap_forecast()` - 第 143 行
   - `_load_weight_profile()` - 第 161 行
   - `_load_delay_profile()` - 第 173 行

2. `v2/api_client.py`
   - 需要添加学习算法相关方法

---

## 结论

**当前对接进度**: 3/9 (33%)

**核心功能**: 已对接 (磅单、报货单、创建报货单)

**缺失功能**: 

- 合同数据 (易解决)
- 仓库发货能力评估 (需新开发)
- 模型预测相关 (需新开发)

**建议**: 优先完成合同数据对接和仓库能力评估模块，这两项对模型准确性影响最大。

---

**报告时间**: 2026-03-08 14:15  
**报告人**: AIIS188
